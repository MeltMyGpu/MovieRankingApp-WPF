<Window x:Class="MovieRankingApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieRankingApplication"
        mc:Ignorable="d"
        DataContext="{Binding GetMainWindowViewModel, Source={StaticResource ViewModelLocator}}"
        Title="MovieRankingApplication" Height="720" Width="1280"
        >

    <Grid x:Name="MainWinSplit">
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="0.2*"/>
            <ColumnDefinition  Width="0.8*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            
        </StackPanel>
        
        <Frame x:Name="PageDisplayFrame" Source="{Binding CurrentPageUri}" Grid.Column="2" />

    </Grid>
</Window>
